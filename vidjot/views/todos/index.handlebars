{{#each todos}}
    <div class="card card-body mb-2">
        <h4>{{title}}</h4>  
        <p>{{task}}</p>
        <p><i>{{created}}</i></p>
        <a class="btn btn-danger" href="/todos/edit/{{id}}">Edit</a>
    </div>
{{else}}
    <h1>No TODO's</h1>
{{/each}}